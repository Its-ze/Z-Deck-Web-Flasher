# Z-Deck 0.2.46-cyberdeck Public Source Patch Archive

Public-safe source patch archive for firmware 2.8.0.zdeck47 / pack 0.2.46-cyberdeck.

This release keeps the right-sidebar battery clearance fix and splits OTA preflight SD backup from the HTTP/download install frame to avoid resets at `ota/preflight.backup.begin`. No private channel URLs, PSKs, admin keys, Wi-Fi passwords, or user device configs are bundled.
