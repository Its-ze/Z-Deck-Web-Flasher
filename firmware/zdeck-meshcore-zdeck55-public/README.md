# MeshCore Companion For zdeck55 Layout

MeshCore companion image for the dedicated `meshcore` app partition at `0xa10000`. It reads the saved Z-Deck A/B slot and validates that image before returning to Z-Deck.

This image is used only by `manifest-dual.json` and `manifest-ota-test.json`. It does not contain user settings or private channels.
