# MeshCore Companion For zdeck57 Layout

MeshCore companion image for the dedicated `meshcore` partition. MeshCore stays active through normal use; returning to Z-Deck requires the explicit press-and-release action on the `Z-DECK` page.

This image contains no user settings, private channels, keys, or chats.
